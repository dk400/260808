# 탐색기 · OS 잔여물
Thumbs.db
desktop.ini
.DS_Store

# 에디터
.vscode/
.idea/

# 임시 파일
*.tmp
~$*

# 이 프로젝트는 의존성 0 정적 HTML이다 (PRD §9.1).
# node_modules / 빌드 산출물이 생겼다면 그건 규칙 위반이지 무시 대상이 아니다.

# 로컬 백업 스냅샷 — git 이 이 역할을 하므로 추적하지 않는다
*.pre-*.html
*.backup-*.html

# 배포 폴더는 scripts/make-deploy.js 가 만드는 생성물이다.
# 복사본을 커밋해두면 원본과 갈라져 어느 쪽이 최신인지 알 수 없게 된다.
deploy/
