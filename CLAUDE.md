# AI Builder Origin — 작업 기준

> 이 파일은 매 세션 자동 로드된다. **작업 시작 전 반드시 이 문서와 `docs/PRD_v1.0.md`를 읽는다.**

---

## 1. 프로젝트 한 줄

```
AI Builder School 을 통과한 빌더의 작업 역량을 보여주고,
외주 문의를 pluuug 으로 전환시키는 신규 브랜드 웹사이트.
```

- **레퍼런스 구현**: `AI_Builder_Origin_v3.html` — 모든 명세의 기준
- **현재 단계**: 시안 제출용 정적 프로토타입 (Phase 1)
- **평가 기준 ①**: AI가 딸깍으로 만든 느낌이 나지 않을 것
- **평가 기준 ②**: 전환 트래킹이 가능한 구조

---

## 2. 🔴 절대 하지 말 것

### 2.1 인터랙션

```
현재 배분:  강 1개 · 중 2개 · 약 5개 · 무 1개
```

| 섹션 | 인터랙션 |
|---|---|
| `#problem` | sticky 문장 교체 — **강** |
| `#hero` `#how` | 스태거 / 순차 강조 — 중 |
| `#proof` `#work` `#insight` `#faq` `#final` | 호버·슬라이드·아코디언 — 약 |
| **`#builders`** | **없음 — 의도된 정적 구간** |

- **`#builders` 에 애니메이션을 넣지 마라.** 빈 구간이 아니라 판별 기준이다.
  > 클라이언트 원문: *"애니메이션을 넣는 판단만큼 안 넣는 판단도 기준이다"*
- 새 섹션에 인터랙션을 자동으로 붙이지 마라. 붙이려면 배분표를 먼저 갱신하고 보고하라.
- 모든 섹션이 같은 방식으로 등장하면 그것이 딸깍 신호다. (반례: Ten AI)

### 2.2 계측 레일 (`.rail`)

**오버레이 방식이다. 콘텐츠를 밀지 않는다.**

```
❌ body { padding-left }
❌ .header { left: ... }
❌ .dock { left: ... }      ← .dock 은 left:50% + translate(-50%) 중앙 플로팅.
                              left 를 덮으면 화면 밖으로 나간다.
✅ 레일은 컨테이너 좌측 여백 위에만 떠 있다
✅ min-width: 1360px 이상에서만 표시
```

레일은 **정지 화면 한 장으로도 "손이 갔다"가 드러나는 유일한 요소**다. 시안 선별이 캡처로 이뤄질 가능성에 대비한 장치이므로 제거하지 마라.

### 2.3 디자인 토큰

```css
--bg:#f4f2eb  --panel:#ebe8df  --ink:#111311  --muted:#686b65
--lime:#d7ff59  --lime-2:#c8ed45  --blue:#b9d8ff  --orange:#ffc49a
--radius:22px  --max:1320px  --pad:clamp(20px,4.2vw,64px)
```

**새 색상·폰트를 추가하지 마라.** 액센트가 늘어나면 그 자체가 AI 생성물의 신호다.

### 2.4 카피

| 금지 | 이유 |
|---|---|
| **가격·저렴함 소구** | 반반외주 실패 사례. *"500만 원으로 이상한 걸 만들 바에 1,000만 원 주고 더 좋은 걸"* |
| **검증 불가 수치** (`100% 만족도` `누적 128건` `재의뢰율 62%`) | 신규 브랜드가 쓰면 허위. 경쟁사가 이미 씀 |
| **"엄격히 선별된 빌더"** | ⚠️ **1기는 전원 동일 진행이다.** 사실과 다름 |
| "업계 1위" "최고" | 검증 불가 |
| **제작 노트를 본문에 쓰기** | 아래 참조 |

#### 🔴 제작 노트를 사이트 카피에 쓰지 마라

실제로 발생한 사고다.

```
❌ "카카오의 순차 강조 방식을 단순화해, 스크롤 위치에 따라
    현재 단계만 선명하게 보이도록 구성했습니다."
```

레퍼런스 이름이 사이트에 노출되면 모방으로 읽힌다. 설계 근거는 `docs/` 에만 쓴다.

```
✅ "각 단계에서 무엇을 확인하고, 무엇을 전달받는지
    미리 정해두고 시작합니다."
```

**안전한 표현**: 근거 · 기준 · 기록 · 공개
**보류 표현**: 통과 · 선별 · 검증된 (단정형)

### 2.5 기술

```
❌ 프레임워크·빌드 도구 도입 (Next.js, Vite, Tailwind CLI 등)
❌ npm 패키지 설치
❌ 인증·DB 연동
❌ 자체 문의 폼 구현 (pluuug 으로 넘긴다)
```

**의존성 0을 유지하는 이유**: 6인 선별 심사에서 누구나 파일을 열어봐야 하고, 빌드 실패 리스크를 없애야 한다.

### 2.6 범위

```
❌ 빌더 자동 매칭 · 배정 시스템 · CRM · 계약 · 결제
❌ 프로젝트 진행 관리 · 교육 LMS · 커뮤니티 게시판
❌ 새 페이지·섹션 추가 (요청받지 않은 것)
❌ 검색 기능
```

경쟁사가 공통으로 쓰는 것도 넣지 마라: **후기 마퀴 · 기술 스택 로고 나열 · 검증 불가 수치.** 현재 v3에 없는 것은 우연이 아니라 설계에서 배제한 결과다.

---

## 3. 반드시 지킬 것

### 3.1 작업 절차

```
1. 관련 파일을 찾는다
2. 현재 동작과 원인을 설명한다
3. 변경 예정 파일을 보고한다
4. 승인 후 수정한다
5. 브라우저 확인 지점을 알려준다
6. 다음 작업으로 넘어가기 전에 보고한다
```

**한 번에 여러 항목을 동시에 고치지 마라.**

### 3.2 접근성 (기존 수준 유지)

```
prefers-reduced-motion: reduce  → 모든 모션 비활성
키보드 포커스 링 유지
장식 요소 aria-hidden="true"    ← ✱ 심벌 포함
아코디언 aria-expanded
터치 타깃 44px 이상
```

### 3.3 반응형

```
1360px ↑   레일 표시
1320px     컨테이너 최대
900px ↓    Work 사이드바 → 가로 필터칩
720px ↓    모바일 메뉴, 단일 컬럼
```

모바일은 특별한 변형 없이 **세로 스택**이다. 똑개 실측과 동일한 전략이다.

### 3.4 전환 트래킹

현재 `data-track` 5종: `header_contact` `hero_contact` `hero_work` `dock_contact` `final_contact`

추가 시 이 규칙을 따른다.
```
work_card_click · insight_click · youtube_out · tab_change · pluuug_out
파라미터: section / content_id / destination / utm_source
```

URL 분기는 **분석 가치가 있는 상태만** 반영한다. 정렬·아코디언 같은 UI 상태는 넣지 마라.

---

## 4. 미확정 — 임의로 정하지 마라

| 항목 | 처리 |
|---|---|
| 브랜드명 (그룹/크루/팀/오리진) | `AI BUILDER ORIGIN` 유지, TODO 주석 |
| 도메인 | 표기하지 않음 |
| **pluuug 폼 URL / 임베드 코드** | `#` 유지 + TODO 주석 |
| **에셋 원본 제공 주체** | 콘셉트 프리뷰 유지 |
| GA4 측정 ID | `<head>` 에 삽입 위치만 주석 |
| Work 표기 방식 (똑개 실적 사용 시) | 임의 표기 금지 |

**추측으로 채우지 말고 TODO 로 남기고 보고하라.**

---

## 5. 참고 문서

| 파일 | 내용 |
|---|---|
| `docs/PRD_v1.0.md` | **단일 기준 문서** |
| `docs/시안설계운영제안서.md` | A4~A7 (Insight 카테고리·유튜브 배치·UX 검토·무드 가이드) |
| `docs/레퍼런스분석.md` | 똑개·빌더스쿨·디펙트럼·리트머스 분석 |
| `docs/녹화실측분석.md` | 화면녹화 8종 프레임 분석 |

문서 간 충돌이 있으면 **임의로 판단하지 말고 충돌 내용을 먼저 보고하라.**

---

## 6. 레퍼런스 역할

```
구조는 똑개 · 전환은 pluuug · 브랜드 표식은 빌더스쿨
```

| 레퍼런스 | 가져올 것 |
|---|---|
| 똑똑한개발자 | IA · Work · Insight · FAQ · 신뢰 구조 |
| pluuug | B2B 전환 카피 · 제품 증명 방식 · 데이터 수집 |
| AI 빌더스쿨 | `✱` 심벌 · 라임 포인트 (연결 장치로만) |
| 토스 · 카카오 | 인터랙션 · 모바일 대체 방식 |
| Plus X | 비주얼 영감 (제한적) |
| 디펙트럼 · 리트머스 | 경쟁 논리 비교 |

**빌더스쿨의 pill 남용·기울인 카드·형광 하이라이터는 계승하지 않는다.** 교육 상품(개인 대상)이라 활기차지만, 우리는 외주 발주(B2B)라 차분하고 정확해야 한다.
